pipeline {
    agent any

    stages {
        stage("build") {
            steps {
                echo "this is the build step"
            }
        }

        stage("test stage") {
            steps {
                echo "this is the test stage"
            }
        }

        stage("deploy") {
            steps {
                echo "this is the deploy stage"
            }
        }
    }
}
